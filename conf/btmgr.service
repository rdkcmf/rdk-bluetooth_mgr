[Unit]
Description=Bluetooth Manager Daemon over IARM IPC
After=lighttpd.service iarmbusd.service
Requires=iarmbusd.service

[Service]
Type=forking
ExecStart=/bin/sh -c "/usr/bin/btMgrBus &"
Restart=always

[Install]
WantedBy=multi-user.target
